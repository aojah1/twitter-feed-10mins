apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: twitter-feed-10mins
  labels:
    commit: ${WERCKER_GIT_COMMIT}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: twitter-feed-10mins
  template:
    metadata:
      labels:
        app: twitter-feed-10mins
        commit: ${WERCKER_GIT_COMMIT}
    spec:
      containers:
      - name: ttwitter-feed-10mins
        image: ${DOCKER_REPO}:${WERCKER_GIT_BRANCH}-${WERCKER_GIT_COMMIT}
        imagePullPolicy: Always
        ports:
        - name: twitter-feed-10mins
          containerPort: ${PORT}
          protocol: TCP
      imagePullSecrets:
        - name: wercker
---
apiVersion: v1
kind: Service
metadata:
  name: twitter-feed-10mins
  labels:
    app: twitter-feed-10mins
    commit: ${WERCKER_GIT_COMMIT}
spec:
  ports:
  - port: 30000
    targetPort: ${PORT}
  selector:
    app: twitter-feed-10mins
  type: ClusterIP
---
